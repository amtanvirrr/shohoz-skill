-- Add DELETE policy for admins (already covered by ALL policy, but let's verify)
-- The existing "Admins can manage orders" policy with ALL command already covers DELETE.
-- No changes needed.
SELECT 1;